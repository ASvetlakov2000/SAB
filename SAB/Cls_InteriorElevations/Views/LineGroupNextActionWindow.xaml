<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SAB Развертки стен"
        Height="270"
        Width="680"
        MinHeight="270"
        MinWidth="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="{DynamicResource SabBrush.WindowBackground}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../UI/Styles/SABWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Style="{StaticResource SabPanelBorderStyle}"
                Margin="20,18,20,14">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Border Style="{StaticResource SabPanelIconBorderStyle}">
                        <TextBlock Text="✓" Style="{StaticResource SabPanelIconTextStyle}" />
                    </Border>
                    <TextBlock x:Name="HeaderTextBlock"
                               Text="Группа №1 добавлена."
                               Style="{StaticResource SabPanelHeaderTextStyle}" />
                </StackPanel>

                <TextBlock Text="Выберите, продолжить сбор групп линий или перейти к выбору помещения."
                           Style="{StaticResource SabFieldHintTextStyle}"
                           TextWrapping="Wrap"
                           LineHeight="18"
                           Margin="0,0,0,14" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button x:Name="NextGroupButton"
                            Grid.Column="0"
                            Content="Выбрать следующую группу линий"
                            Height="40"
                            Style="{StaticResource SabOutlineButtonStyle}" />

                    <Button x:Name="FinishButton"
                            Grid.Column="2"
                            Content="Завершить выбор групп"
                            Height="40"
                            Style="{StaticResource SabPrimaryButtonStyle}" />
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource SabBrush.PanelBackground}"
                BorderBrush="{StaticResource SabBrush.Border}"
                BorderThickness="0,1,0,0"
                Padding="20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelButton"
                        Content="Отмена"
                        Width="120"
                        Style="{StaticResource SabNeutralButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
