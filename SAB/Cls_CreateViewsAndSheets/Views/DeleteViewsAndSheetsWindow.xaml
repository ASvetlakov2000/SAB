<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SAB Удаление видов и листов"
        Height="720"
        Width="1120"
        MinHeight="560"
        MinWidth="920"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        FontFamily="Segoe UI"
        FontSize="13"
        Background="{DynamicResource SabBrush.WindowBackground}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../UI/Styles/SABWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="0,0,0,14">
            <Border Style="{StaticResource SabPanelIconBorderStyle}">
                <TextBlock Text="×" Style="{StaticResource SabPanelIconTextStyle}" />
            </Border>
            <TextBlock Text="Удаление видов и листов"
                       Style="{StaticResource SabPanelHeaderTextStyle}" />
        </StackPanel>

        <Border Grid.Row="1"
                Style="{StaticResource SabDataGridContainerBorderStyle}">
            <DataGrid x:Name="DeletionRowsDataGrid"
                      ItemsSource="{Binding Rows}"
                      Style="{StaticResource SabDataGridStyle}"
                      BorderThickness="0"
                      CanUserResizeColumns="True"
                      IsReadOnly="False"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header=""
                                            Width="58"
                                            MinWidth="58"
                                            MaxWidth="58"
                                            CanUserResize="False"
                                            CellStyle="{StaticResource SabDataGridCellStyle}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          ToolTip="Удалить лист и размещенные на нем виды" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="№"
                                        Binding="{Binding RowNumber}"
                                        IsReadOnly="True"
                                        Width="60"
                                        MinWidth="52"
                                        ElementStyle="{StaticResource SabDataGridCenteredTextBlockStyle}" />

                    <DataGridTextColumn Header="№ листа"
                                        Binding="{Binding SheetNumber}"
                                        IsReadOnly="True"
                                        Width="150"
                                        MinWidth="120"
                                        ElementStyle="{StaticResource SabDataGridTextBlockStyle}" />

                    <DataGridTextColumn Header="Имя листа"
                                        Binding="{Binding SheetName}"
                                        IsReadOnly="True"
                                        Width="260"
                                        MinWidth="180"
                                        ElementStyle="{StaticResource SabDataGridTextBlockStyle}" />

                    <DataGridTextColumn Header="Размещенные виды"
                                        Binding="{Binding PlacedViewsText}"
                                        IsReadOnly="True"
                                        Width="*"
                                        MinWidth="360"
                                        ElementStyle="{StaticResource SabDataGridTextBlockStyle}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <TextBlock Text="{Binding StatusText}"
                           Style="{StaticResource SabFieldHintTextStyle}"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0" />
                <Button Content="Выбрать все"
                        Command="{Binding SelectAllCommand}"
                        Style="{StaticResource SabOutlineButtonStyle}"
                        Margin="0,0,8,0" />
                <Button Content="Снять выбор"
                        Command="{Binding ClearSelectionCommand}"
                        Style="{StaticResource SabNeutralButtonStyle}" />
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Отмена"
                        Width="120"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SabNeutralButtonStyle}"
                        Margin="0,0,10,0" />
                <Button Content="Удалить"
                        Width="130"
                        Command="{Binding DeleteCommand}"
                        IsEnabled="{Binding CanDelete}"
                        Style="{StaticResource SabDangerOutlineButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
