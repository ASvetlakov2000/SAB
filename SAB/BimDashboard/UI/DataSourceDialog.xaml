<Window x:Class="SAB.BimDashboard.UI.DataSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Источник данных dashboard"
        Width="520"
        Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"
                   Text="Выберите источник данных для HTML dashboard:"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <RadioButton x:Name="RevitRadioButton"
                         Margin="0,0,0,6"
                         IsChecked="True"
                         Content="Текущая модель Revit"
                         Checked="SourceRadioButton_Checked"/>
            <RadioButton x:Name="CsvRadioButton"
                         Margin="0,0,0,6"
                         Content="CSV файл"
                         Checked="SourceRadioButton_Checked"/>
            <RadioButton x:Name="ExcelRadioButton"
                         Content="Excel файл (XLSX)"
                         Checked="SourceRadioButton_Checked"/>
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,0,0,6"
                   Text="Путь к файлу (для CSV/XLSX):"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="FilePathTextBox"
                     Grid.Column="0"
                     Height="28"
                     VerticalContentAlignment="Center"
                     IsEnabled="False"/>

            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    Width="100"
                    Height="28"
                    Content="Обзор"
                    IsEnabled="False"
                    Click="BrowseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="4"
                    Margin="0,14,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="OkButton"
                    Width="100"
                    Height="30"
                    Margin="0,0,8,0"
                    Content="Построить"
                    Click="OkButton_Click"/>
            <Button x:Name="CancelButton"
                    Width="100"
                    Height="30"
                    Content="Отмена"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
